import { createContext } from "react";

//Con el contexto podemos exponer información a cualquier componente que quiera tomar información de el

// Creamos un react componente que cargamos el contexto.
export const UserContext = createContext(null);
